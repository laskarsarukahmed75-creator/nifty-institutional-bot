# Core
smartapi-python==1.5.5
Flask==3.0.3
gunicorn==21.2.0
requests==2.31.0

# Transitive dependencies (pinned for Python 3.12 compatibility)
cryptography==41.0.7          # smartapi-python requires older versions
pycryptodome==3.20.0          # same
python-dotenv==1.0.1          # for environment loading (if used)
urllib3==2.0.7                # avoid v2.1+ issues
certifi==2024.2.2
charset-normalizer==3.3.2
idna==3.6
